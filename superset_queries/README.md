# SQL-запросы для Superset

## Отчет: Реактивации пользователей

### Варианты запросов:

1. **`reactivations_by_period_simple.sql`** (РЕКОМЕНДУЕТСЯ)
   - Детальная таблица всех реактиваций
   - Упрощенный запрос БЕЗ параметров
   - Используйте фильтры Dashboard для выбора периода
   - Показывает: user_id, first_deposit, prev_deposit_date, days_inactive, inactivity_period
   - Визуализации: Table, Bar Chart, Pie Chart

2. **`reactivations_summary_by_period_simple.sql`** (РЕКОМЕНДУЕТСЯ для графиков)
   - Сводка реактиваций по периодам неактивности
   - Упрощенный запрос БЕЗ параметров
   - Используйте фильтры Dashboard для выбора периода
   - Показывает: количество реактиваций, средние/мин/макс дни неактивности, проценты
   - Визуализации: Bar Chart, Pie Chart, Table

3. **`reactivations_by_period.sql`**
   - Детальная таблица с параметрами (start_date, end_date)
   - Для продвинутых пользователей

4. **`reactivations_summary_by_period.sql`**
   - Сводка с параметрами (start_date, end_date)
   - Для продвинутых пользователей

### Что показывает:
- Количество реактиваций в разрезе периодов неактивности:
  - **7-14 days**: Реактивации через 1-2 недели
  - **14-30 days**: Реактивации через 2-4 недели
  - **30-90 days**: Реактивации через 1-3 месяца
  - **90+ days**: Реактивации через 3+ месяца
- Средние, минимальные и максимальные дни неактивности
- Процентное распределение по периодам

### Как использовать в Superset:

1. **SQL Lab**: 
   - Откройте **SQL** → **SQL Lab**
   - Выберите базу данных: **"Ubidex Events DB"**
   - Вставьте SQL-запрос из `reactivations_summary_by_period_simple.sql`
   - Нажмите **"Run"**

2. **Создайте Chart**:
   - Нажмите **"Explore"** рядом с результатами
   - Выберите тип визуализации:
     - **Bar Chart** - для графика по периодам
     - **Pie Chart** - для распределения
     - **Table** - для табличного представления
   - Настройте:
     - **Group by**: `inactivity_period`
     - **Metrics**: `SUM(reactivations_count)`, `AVG(avg_days_inactive)`
   - Нажмите **"Create chart"**

3. **Создайте Dashboard**:
   - Сохраните Chart
   - Добавьте фильтр **Date Range Filter** для `first_deposit`
   - Настройте Scoping для применения фильтра к Chart

### Скрипт для расчета реактиваций по списку пользователей:

Для расчета реактиваций по конкретному списку пользователей используйте скрипт `calculate_reactivations_by_user_list.py`:

```bash
# Из CSV файла (с колонкой user_id или external_user_id)
docker exec -it ubidex_analysis python scripts/calculate_reactivations_by_user_list.py \
  --users data/user_list.csv \
  --start-date 2025-11-01 \
  --end-date 2025-11-30

# Из списка ID (через запятую)
docker exec -it ubidex_analysis python scripts/calculate_reactivations_by_user_list.py \
  --users "user1,user2,user3" \
  --start-date 2025-11-01 \
  --end-date 2025-11-30 \
  --output my_reactivations
```

Скрипт создаст два CSV файла:
- `*_detail.csv` - детальная информация по каждой реактивации
- `*_summary.csv` - сводка по периодам неактивности

---

## Отчет: Коэффициенты паблишеров в разрезе форматов

### Варианты запросов:

1. **`publisher_coefficients_by_period_simple.sql`** (РЕКОМЕНДУЕТСЯ)
   - Упрощенный запрос БЕЗ параметров
   - Используйте фильтры Dashboard для выбора периода
   - Работает с данными о расходах из `publisher_spend`
   - Поддерживает несколько месяцев

2. **`publisher_coefficients_by_period.sql`**
   - С параметрами в SQL (start_date, end_date, min_spend)
   - Для продвинутых пользователей
   - Работает с данными о расходах из `publisher_spend`

3. **`publisher_coefficients_by_day_simple.sql`** (РЕКОМЕНДУЕТСЯ для дневных данных)
   - Разбивка по дням БЕЗ параметров
   - Используйте фильтры Dashboard для выбора периода
   - Требует таблицу `publisher_spend_daily`

4. **`publisher_coefficients_by_day.sql`**
   - Разбивка по дням с параметрами
   - Требует таблицу `publisher_spend_daily`
   - Параметры: start_date, end_date, min_spend

5. **`publisher_coefficients_november_by_format_simple.sql`**
   - Только ноябрь 2025 (без параметров)
   - Для быстрого старта

6. **`publisher_coefficients_november_by_format.sql`** (полный)
   - Использует данные о расходах И депозитах
   - Требует миграции `user_events`

## Загрузка дневных данных

Для загрузки дневных CSV файлов используйте скрипт `load_daily_spend_to_postgresql.py`:

```bash
# Загрузить один файл (дата определяется из имени файла)
docker exec -it ubidex_analysis python scripts/load_daily_spend_to_postgresql.py --csv data/spend_2025-11-01.csv

# Загрузить несколько файлов
docker exec -it ubidex_analysis python scripts/load_daily_spend_to_postgresql.py --csv data/spend_2025-11-01.csv data/spend_2025-11-02.csv

# Загрузить все CSV из директории
docker exec -it ubidex_analysis python scripts/load_daily_spend_to_postgresql.py --dir data/spend_daily/

# Указать дату вручную
docker exec -it ubidex_analysis python scripts/load_daily_spend_to_postgresql.py --csv data/spend.csv --date 2025-11-01
```

Скрипт автоматически:
- Определяет дату из имени файла (поддерживает форматы: 2025-11-01, 20251101, 01-11-2025)
- Создает таблицу `publisher_spend_daily` если её нет
- Удаляет дубликаты при повторной загрузке
- Показывает статистику загруженных данных

Все запросы повторяют логику из скрипта `calculate_bid_coefficients.py`.

📖 **Подробное руководство**: См. [DASHBOARD_GUIDE.md](DASHBOARD_GUIDE.md) для создания Dashboard с фильтрами.

### Что показывает:
- Коэффициенты для каждого паблишера
- Группировка по форматам (POP, PUSH, VIDEO, BANNER, NATIVE)
- Целевые CPA по форматам (рассчитываются на основе PLR/NOR кампаний - 30%)
- Текущие CPA
- Рекомендации по изменению ставок

### Как использовать в Superset:

1. **Откройте Superset**: http://localhost:8088
   - Логин: `admin`
   - Пароль: `admin`

2. **SQL Lab**: 
   - В верхнем меню нажмите **SQL** → **SQL Lab**

3. **Выберите базу данных**: 
   - В выпадающем списке выберите **"Ubidex Events DB"**

4. **Вставьте SQL-запрос**:
   - Откройте файл `publisher_coefficients_november_by_format_simple.sql`
   - Скопируйте весь SQL-запрос
   - Вставьте в редактор SQL Lab

5. **Выполните запрос**:
   - Нажмите кнопку **"Run"** (или Ctrl+Enter)
   - Дождитесь результатов

6. **Создайте визуализацию**:
   - Нажмите кнопку **"Explore"** рядом с результатами
   - Выберите тип визуализации:
     - **Table** - для табличного представления
     - **Pivot Table** - для группировки по форматам
   - Настройте метрики и группировки:
     - **Group by**: `format`, `publisher_name`
     - **Metrics**: `SUM(spend)`, `AVG(coefficient)`, `AVG(current_cpa)`
   - Нажмите **"Create chart"**

### Требования:
- Таблица `publisher_spend` с данными о расходах (уже загружена)
- Таблица `user_events` с данными о депозитах (нужно мигрировать)

### Миграция данных user_events:

Если данные еще не мигрированы, выполните:

```bash
# Скопируйте events.db в директорию data/
copy C:\Users\Nalivator3000\superset-data-import\events.db data\events.db

# Запустите миграцию
docker exec -it ubidex_analysis python scripts/migrate_to_postgresql.py
```

