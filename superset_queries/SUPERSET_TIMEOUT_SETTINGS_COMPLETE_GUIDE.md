# Полное руководство: Настройка таймаутов в Superset

## Проблема

```
Timeout error: We're having trouble loading this visualization. 
Queries are set to timeout after 60 seconds.
```

---

## ⚠️ ВАЖНО: Где находятся настройки

В Superset настройки таймаута находятся в **разных местах** в зависимости от версии и типа запроса:

1. **SQL Lab queries** → настраиваются в `superset_config.py` (уже настроено)
2. **Chart queries** → настраиваются в UI в настройках Database
3. **Dashboard queries** → используют настройки Chart

---

## Решение 1: Настройка через Superset UI (для Chart queries)

### Шаг 1: Откройте настройки Database

1. Войдите в Superset
2. В верхнем меню найдите **"Data"** или **"⚙️ Settings"** → **"Database Connections"**
3. Найдите вашу базу данных: **"Ubidex Events DB"**
4. **Кликните на название** или на иконку **✏️ Edit** (карандаш)

### Шаг 2: Найдите раздел "Advanced" или "Additional Settings"

Настройки могут находиться в разных местах в зависимости от версии Superset:

#### Вариант A: Вкладка "Advanced"
- После открытия редактирования Database, прокрутите вниз
- Найдите вкладку или раздел **"Advanced"** или **"Additional Settings"**
- Кликните на него

#### Вариант B: Прямо на странице редактирования
- Прокрутите страницу редактирования Database вниз
- Ищите разделы:
  - **"Query Execution Options"**
  - **"Performance"**
  - **"Advanced Settings"**
  - **"Additional Settings"**

#### Вариант C: Через кнопку "Show Advanced Options"
- Внизу страницы редактирования может быть кнопка **"Show Advanced Options"** или **"Advanced"**
- Кликните на неё, чтобы раскрыть дополнительные настройки

### Шаг 3: Включите асинхронные запросы

В разделе **"Query Execution Options"** или **"Performance"** найдите:

- ✅ **"Asynchronous query execution"** - включите этот чекбокс
- Или **"Allow async query execution"**
- Или **"Enable async queries"**

**Это критически важно!** Без этого Chart queries будут таймаутиться через 60 секунд.

### Шаг 4: Установите Query Timeout (если доступно)

В том же разделе найдите поле:

- **"Query Timeout"** 
- Или **"SQL Query Timeout"**
- Или **"Query execution timeout"**

Введите значение: **600** (10 минут в секундах)

Если поля нет - не страшно, асинхронные запросы решат проблему.

### Шаг 5: Сохраните изменения

1. Нажмите кнопку **"Save"** или **"Update"** внизу страницы
2. Дождитесь подтверждения: "Database updated successfully"

---

## Решение 2: Настройка через SQL Lab (для SQL Lab queries)

Настройки для SQL Lab уже применены в `superset_config.py`:

```python
SQLLAB_TIMEOUT = 600  # 10 minutes
SQLLAB_ASYNC_TIME_LIMIT_SEC = 600  # 10 minutes
```

**Проверка:**
1. Откройте **SQL Lab** → **SQL Editor**
2. Выполните длинный запрос
3. Он должен работать дольше 60 секунд

---

## Решение 3: Если не можете найти настройки в UI

### Альтернативный способ: Настройка через Dataset

1. Откройте **Data** → **Datasets**
2. Найдите Dataset, который использует проблемный Chart
3. Кликните на Dataset для редактирования
4. Найдите раздел **"Advanced"** или **"Performance"**
5. Включите **"Async query execution"** если доступно

### Альтернативный способ: Настройка через Chart

1. Откройте Chart, который таймаутится
2. Кликните **"Edit"** (карандаш)
3. Найдите раздел **"Query"** или **"Advanced"**
4. Включите **"Async query"** если доступно

---

## Решение 4: Настройка на уровне PostgreSQL (для опытных)

Если у вас есть доступ к PostgreSQL, можно установить таймаут глобально:

```sql
-- Подключитесь к вашей базе данных Railway PostgreSQL
ALTER DATABASE railway SET statement_timeout = '600000';  -- 10 минут в миллисекундах
```

**Внимание:** Это повлияет на ВСЕ запросы к этой базе данных.

---

## Решение 5: Проверка версии Superset

Если настройки не найдены, возможно, у вас старая версия Superset:

1. В Superset найдите **"⚙️ Settings"** → **"About"** или **"Info"**
2. Проверьте версию Superset
3. Если версия < 2.0, некоторые настройки могут отсутствовать

**Решение:** Обновите Superset до последней версии или используйте альтернативные методы.

---

## Пошаговая инструкция с поиском по интерфейсу

### Если вы не видите раздел "Advanced":

1. **Попробуйте поиск на странице:**
   - Нажмите `Ctrl+F` (или `Cmd+F` на Mac)
   - Введите: `async` или `timeout` или `query execution`
   - Это поможет найти нужные настройки

2. **Проверьте все вкладки:**
   - На странице редактирования Database могут быть вкладки:
     - **"Basic"** или **"Connection"**
     - **"Advanced"** или **"Additional"**
     - **"Performance"**
     - **"Security"**
   - Проверьте каждую вкладку

3. **Проверьте выпадающие меню:**
   - Могут быть кнопки **"Show more"** или **"Expand"**
   - Кликните на них, чтобы раскрыть дополнительные настройки

---

## Визуальные подсказки: Что искать

Ищите следующие элементы на странице:

- ☑️ **Чекбоксы** с текстом про "async", "asynchronous", "background"
- 🔢 **Числовые поля** с названием "timeout", "query timeout", "execution timeout"
- 📋 **Разделы** с названием "Query Execution", "Performance", "Advanced"
- 🔽 **Кнопки** "Show Advanced", "More Options", "Expand"

---

## Проверка результата

После настройки:

1. **Откройте Chart**, который раньше таймаутился
2. **Запрос должен выполняться** дольше 60 секунд без ошибки
3. **Если запрос долгий**, вы увидите:
   - Индикатор загрузки (спиннер)
   - Сообщение "Query is running..."
   - Вместо ошибки таймаута

---

## Если ничего не помогло

### Вариант 1: Создайте новый Database Connection

1. **Data** → **Databases** → **+ Database**
2. Создайте новое подключение с теми же параметрами
3. В процессе создания проверьте все настройки
4. Используйте новое подключение для Dataset

### Вариант 2: Используйте SQL Lab вместо Chart

1. Откройте **SQL Lab** → **SQL Editor**
2. Выполните запрос напрямую
3. SQL Lab использует настройки из `superset_config.py` (уже настроено)

### Вариант 3: Упростите запрос

1. Добавьте фильтры по дате в SQL запрос
2. Уменьшите объем данных
3. Используйте агрегацию вместо детальных данных

---

## Техническая информация

### Какие настройки где находятся:

| Тип запроса | Где настраивается | Текущий статус |
|------------|------------------|----------------|
| SQL Lab | `superset_config.py` → `SQLLAB_TIMEOUT` | ✅ Настроено (600 сек) |
| SQL Lab Async | `superset_config.py` → `SQLLAB_ASYNC_TIME_LIMIT_SEC` | ✅ Настроено (600 сек) |
| Chart queries | UI → Database → Advanced → Async execution | ⚠️ Нужно настроить вручную |
| Chart timeout | UI → Database → Advanced → Query Timeout | ⚠️ Опционально |

---

## Резюме: Что делать прямо сейчас

1. ✅ **Откройте Superset** → **Data** → **Databases**
2. ✅ **Кликните на "Ubidex Events DB"** для редактирования
3. ✅ **Прокрутите вниз** и найдите раздел **"Advanced"** или **"Query Execution Options"**
4. ✅ **Включите чекбокс** ✅ **"Asynchronous query execution"**
5. ✅ **Нажмите "Save"**

**Это должно решить проблему с таймаутом 60 секунд!**

---

## Нужна помощь?

Если вы все еще не можете найти настройки:

1. **Сделайте скриншот** страницы редактирования Database
2. **Проверьте версию Superset** в Settings → About
3. **Попробуйте поиск** на странице (`Ctrl+F`) по словам: `async`, `timeout`, `query`

---

## Дополнительные ресурсы

- Официальная документация Superset: https://superset.apache.org/docs/
- Настройки Database: https://superset.apache.org/docs/databases/installing-database-drivers

